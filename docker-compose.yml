services:

  quexs:
    image: acspri/quexs:2.5.1
    ports:
      - 8080:80
    environment:
      QUEXS_DB_PASSWORD: example
      QUEXS_PORT: :8080

  mysql:
    image: mariadb:10.5
    environment:
      MYSQL_ROOT_PASSWORD: example
